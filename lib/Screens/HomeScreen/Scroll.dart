import 'package:flutter/material.dart';

class ScrollPage extends StatelessWidget {
  const ScrollPage({super.key});
  static String id = 'ScrollPage';

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}